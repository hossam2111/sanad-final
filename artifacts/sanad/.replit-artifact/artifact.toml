kind = "web"
previewPath = "/"
title = "SANAD — National AI Health Platform"
version = "1.0.0"
id = "sanad_health_platform_v3"

[[services]]
localPort = 5173
name = "web"
paths = ["/"]

[services.development]
run = "PORT=5173 BASE_PATH=/ pnpm --filter @workspace/sanad run dev"

[services.production]
serve = "static"

[services.production.build]
args = ["pnpm", "--filter", "@workspace/sanad", "run", "build"]

[services.production.build.env]
NODE_ENV = "production"
BASE_PATH = "/"
